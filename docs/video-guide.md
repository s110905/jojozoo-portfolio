# 作品示範影片：錄製與去敏流程

案例頁只要在 `public/videos/` 找到 `<slug>.mp4`，就會自動在標題下方長出示範影片區塊，
所以流程只有兩步：**產出影片 → 放進 `public/videos/` → 重新 build**。

## 一、目前已完成

| 案例 | 來源 | 說明 |
|---|---|---|
| `01-summer-camp` | 自動錄製 | 正式報名頁巡覽，含報名表單、日曆、匯款流程、進度查詢 |
| `09-jojozoocart` | 自動錄製 | 正式站即時車輛狀態、選車與租借表單流程（手機版面） |
| `11-mothersday-lottery` | 自動錄製 | 活動頁保留畫面 |

重跑指令：

```bash
node scripts/record-demos.mjs                 # 全部
node scripts/record-demos.mjs 09-jojozoocart  # 單一
```

錄製腳本有兩道保護，改動線時不要拆掉：

1. 動線只瀏覽、不送出，避免在正式系統產生真實紀錄。
2. `page.route` 攔截並中止所有非 GET 請求，寫入在瀏覽器層就出不去；真的攔到會印在主控台。

另外 `01-summer-camp` 的動線**刻意跳過 y=720~2416 的活動剪影相簿**，那裡有可辨識的
遊客與兒童臉孔。頁面改版導致區塊位移時，這段座標要重新量。

## 二、還需要你自己錄的

這些需要登入、需要本機環境，或內容本身有個資，我無法代錄。括號內是該案例最值得拍的畫面，
以及錄之前要先處理掉的東西。

| 案例 | 建議拍什麼 | 錄之前先處理 |
|---|---|---|
| `02-payment-system` | 登入 → 權限矩陣 → 款項審核走一輪 | 建測試帳號與假單據，不要用真實員工帳號 |
| `03-hotel-partner-system` | 飯店端產券 → 園區掃碼 → 分次扣額 | 用 demo 帳號＋測試飯店，避免真實合作夥伴名稱 |
| `04-children-drawing-contest` | 投稿流程 → 線上藝廊 | 童畫作者姓名、學校、家長聯絡方式一律遮 |
| `05-ticket-price-calculator` | 輸入人數組合 → 算出最省票價 | 無個資，最好錄的一支 |
| `06-erp-automation-spider` | 終端機執行 → Google Sheet 自動更新 | 遮登入帳號、API 金鑰、試算表網址 |
| `08-pos-autoclicker` | 工具視窗設定 → 實際點擊 POS | POS 畫面若有客單資料要遮 |
| `10-line-lucky-draw` | 手機錄 LINE 加好友 → 抽獎 → 中獎券 | 遮 LINE 顯示名稱與大頭貼 |
| `12-webar-park-guide` | 手機錄實景導航與定位 | 遮入鏡路人臉孔 |
| `13-customer-service-automation` | n8n 流程執行 → Google Sheet 審核台 → 核准發布 | 評論者姓名、頭像、電話一律遮；Sheet 先切到測試分頁 |

## 三、錄影設定

- **解析度**：桌機錄 1280×720；手機錄直接用手機原生尺寸即可，處理腳本會置中並補上網站底色。
- **長度**：15～25 秒。超過 30 秒沒人看完。
- **聲音**：不需要，輸出一律去除音軌。
- **畫面整潔**：關掉通知、書籤列、其他分頁；瀏覽器分頁標題與網址列會入鏡，注意裡面有沒有帳號資訊。
- **游標動線**：慢一點、停頓明確，快速亂晃在壓縮後會變成一團模糊。

## 四、去敏原則

採用的標準是**個資全遮、營運數字保留**：

- 一律遮：顧客與員工姓名、電話、Email、身分證字號、地址、帳號、金鑰、可辨識的臉孔。
- 保留：金額、數量、梯次、使用時間、筆數這類營運數據——這些正是作品的說服力來源。

**最有效的去敏是錄之前就沒有機敏資料**：用測試帳號、測試分頁、假資料。事後遮蔽是補救，不是首選。

## 五、後製指令

```bash
# 1. 先看座標：產生一張疊了格線的截圖（紅線每 100 px、黃線每 500 px，從左上角起算）
node scripts/process-video.mjs 錄好的檔案.mp4 03-hotel-partner-system --probe --poster 5

# 2. 正式輸出：裁切時間、遮掉指定區域
node scripts/process-video.mjs 錄好的檔案.mp4 03-hotel-partner-system \
  --trim 4,26 \
  --hide 300,150,420,60 \
  --hide 880,400,300,80@6-14 \
  --poster 3
```

參數：

| 參數 | 用途 |
|---|---|
| `--trim a,b` | 只保留第 a 到第 b 秒 |
| `--hide x,y,w,h` | 遮蔽一塊區域，可重複使用 |
| `--hide x,y,w,h@a-b` | 只在第 a 到 b 秒之間遮（欄位捲動時用） |
| `--poster 秒` | 指定封面取自第幾秒（預設 1） |
| `--trim` 之外的畫質 | `--fps`（預設 24）、`--crf`（預設 28，數字越大檔越小） |

遮蔽用的是先降取樣再放大回去的馬賽克，不是單純模糊，還原不出原本的文字。
座標以**原始影片畫素**為準，不是輸出後的 1280×720。

輸出會產生三個檔：`.mp4`（H.264）、`.webm`（VP9）、`.jpg`（封面）。

## 六、放上網站

1. 確認 `public/videos/<slug>.mp4` 已存在。
2. 到 `scripts/build-cases.mjs` 的 `DEMO_NOTES` 加一行說明，老實交代這支是怎麼錄的
   （測試資料？正式系統？只瀏覽沒送出？）。沒設定的話會用預設字串。
3. `pnpm build`。

影片是 `preload="none"` + 點擊才播，不會影響案例頁的載入速度；封面圖是唯一會先下載的東西。

`video-raw/` 是原始錄影檔，已經在 `.gitignore` 裡，不會進版控——**原始檔通常還沒去敏，不要提交**。
